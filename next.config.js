/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    compiler: {
      styledComponents: true, // Habilita styled-components sin necesidad de Babel
    },
  };
  
  module.exports = nextConfig;
  